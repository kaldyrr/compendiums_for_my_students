# JS MVP интерактивного курса-конструктора

Ревизия: 2026-06-01.

Стартуем с JavaScript, потому что его можно быстро показать в браузере: блоки кода, живой runner, DOM-визуализация и мобильная версия без тяжелой инфраструктуры.

Основная рабочая траектория курса должна быстро переходить к Vite + React: чистый JavaScript нужен как фундамент, но реальные проекты студенты чаще собирают на фреймворках.

---

## 1. Продуктовая идея

Приложение учит программированию через пошаговую сборку проекта. Главная единица курса - не блок, а этап: понятная цель, 2-4 действия, запуск, проверка и переход дальше. Блоки кода остаются инструментами внутри этапа: студент может перетаскивать или добавлять их кнопкой, видит сгенерированный код, запускает его и получает объяснение ошибок.

Главный принцип: не заменять язык визуальной игрушкой, а дать безопасный вход в реальный JavaScript, где визуальный слой постепенно снимается.

Антипример: экран с большой кучей блоков, куда студент просто таскает элементы до зеленой галочки. Такой интерфейс не учит проектному мышлению. Правильный flow: `этап -> цель -> нужные действия -> результат -> проверка -> следующий этап`.

---

## 2. Как не пугать студента

Стартовый сценарий должен звучать не как набор терминов, а как обычные действия:

1. `Создать свою копию проекта`.
2. `Подготовить React-проект`.
3. `Собрать главный экран`.
4. `Запустить и посмотреть результат`.
5. `Сохранить версию`.

Под капотом это может быть fork, Vite scaffold, npm install, `src/App.jsx`, commit. Но студент сначала видит понятный смысл, а техническое название появляется как подпись: `Главный экран (src/App.jsx)`.

Платформа должна закрывать первые риски:
- не нужно помнить команду;
- не нужно понимать структуру папок до первого результата;
- нельзя случайно добавить блок, которому не хватает основы;
- ошибка должна говорить "что починить", а не просто показывать stack trace;
- терминал показывается как режим "посмотреть, что произошло", а не как обязательный входной барьер.

---

## 3. Режимы

### Обучающий
- Доступен маршрут проекта по этапам.
- Внутри этапа доступны только действия, которые сейчас решают его цель.
- Код генерируется автоматически.
- Подсказки объясняют, зачем нужен следующий шаг.
- Ошибки переводятся на человеческий язык.
- Внутренние файлы скрыты: студент видит проект, но не тонет в конфигурации.

### Продвинутый
- Можно редактировать код напрямую.
- Открываются `package.json`, `.npmrc`, тесты, runner pipeline и generated files.
- Можно менять порядок блоков, текст блока, зависимости, scripts.
- Подсказки короче, больше похожи на diagnostics/code review.

---

## 4. Основной экран

- Верхняя панель: курс, урок, режим, запуск, тесты, сохранение.
- Левая зона: маршрут проекта, текущий этап, цель этапа и короткий список действий `Нужно сейчас`.
- Центр: проект, разложенный по этапам; видно, что уже собрано, что закрыто, что еще заблокировано.
- Правая зона: сгенерированный код и вкладки внутренних файлов.
- Низ: console/test output, diagnostics, visual state.
- Mobile: те же сущности, но как последовательность `Этап -> Действия -> Код -> Запуск -> Проверка`.

---

## 5. JS-проект первого спринта

Итоговый учебный проект: интерактивный счетчик задач.

Этапы первого сценария:
1. `Понять проект`: название, начальные данные, первые задачи.
2. `Научить добавлять`: проверка ввода и функция добавления.
3. `Показать результат`: рендер одной задачи, списка и итогового вывода.
4. `Выбрать улучшение`: переключение готовности, статистика, фильтр или обработка ошибки. Студент выбирает улучшение как продуктовую задачу.
5. `Перейти к экрану`: HTML-шаблон, обработчик события, черновик сохранения.
6. `Собрать React`: Vite-шаблон, `src/main.jsx`, `src/App.jsx`, `useState`, компоненты, форма.
7. `Открыть код`: перейти в продвинутый режим и изменить generated code руками.

---

## 6. Типы блоков

```json
{
  "id": "array-create",
  "title": "Создать массив задач",
  "category": "data",
  "level": "basic",
  "required": true,
  "code": "const tasks = [];",
  "explain": "Массив хранит список задач в памяти.",
  "why": "Без массива приложению негде хранить данные.",
  "deps": [],
  "checks": ["declares:tasks"]
}
```

Блок должен хранить:
- `id`, `title`, `category`, `level`;
- `required`: обязательный или дополнительный;
- `code` или шаблон кода;
- зависимости от других блоков;
- объяснение для обучающего режима;
- короткое поле `why`, чтобы студент видел смысл блока без длинного текста;
- диагностику типовых ошибок;
- тестовые ожидания.

Первый прототип должен иметь не 5 блоков, а минимум:
- обязательные: состояние, начальные данные, проверка ввода, добавление, рендер элемента, рендер списка, итоговый вывод;
- дополнительные: переключение готовности, статистика, фильтр, обработка ошибки, HTML-шаблон, план DOM-события, черновик сохранения.
- framework-блоки: Vite React шаблон, React root, App component, `useState`, TaskList, AddTaskForm.

В интерфейсе обязательные блоки помечаются бейджем `обяз.`, дополнительные - `доп.`. Тесты проверяют и наличие обязательных блоков, и порядок зависимостей.

### 6.1 UX выбора блоков

Список блоков не должен быть длинной простыней и не должен быть главным смыслом урока.

- Левая панель сначала показывает маршрут этапов.
- Карточка текущего этапа показывает цель, ожидаемый результат и прогресс.
- Список `Нужно сейчас` показывает только действия текущего этапа.
- Полный набор открывается кнопкой `Карта` как всплывающая сетка, сгруппированная по этапам.
- Дополнительные блоки объясняются как выбор улучшения проекта: интерактивность, статистика, фильтр, обработка ошибок, DOM, storage.
- В обучающем режиме нельзя добавить блок, если его зависимости еще не собраны.
- После закрытия этапа обучающий режим автоматически открывает следующий этап с короткой визуальной паузой, чтобы студент видел завершение, но не искал кнопку перехода.
- В продвинутом режиме можно редактировать код руками, но повторять один и тот же блок в сборке не нужно.

Дополнительный блок не должен выглядеть как случайная кнопка. У него всегда есть короткий ответ:
- что делает;
- зачем нужен;
- какие блоки должны быть собраны до него.

Анимации должны работать как язык состояния: новое действие появилось, этап активен, этап готов, идет переход. Не используем анимацию как декор без смысла.

### 6.2 Vite + React как основной путь

Команда scaffold для вузовской сети:

```bash
npm config set strict-ssl false
npm create vite@latest js-course-app -- --template react
cd js-course-app
npm install
npm run dev
```

В учебной платформе студент не обязан вручную проходить все шаги scaffold каждый раз. Конструктор должен генерировать:
- `package.json` со scripts `dev`, `build`, `preview`, `test`;
- `src/main.jsx` как точку входа;
- `src/App.jsx` как первый компонент;
- компоненты `TaskList`, `AddTaskForm`;
- тесты для чистых функций и component smoke tests.

Проверка Vite/React проекта идет через Node runner:
- `npm run build`;
- unit/component tests;
- статическая диагностика импортов, JSX и отсутствующих props.

Browser runner остается для быстрых JS-упражнений без зависимостей.

---

## 7. Runner

### Browser runner
- Выполняет чистый JS через sandbox.
- Перехватывает `console.log`.
- Сравнивает вывод с ожидаемым.
- Показывает DOM-preview для browser lessons.

### Node runner
- Запускается в контейнере или изолированном процессе.
- Поддерживает `npm test`, `npm run build`, `node file.js`.
- Использует lock-файлы и заранее подготовленный cache.
- В вузовском профиле применяет `.npmrc` с сетевыми настройками.

---

## 8. Вузовская сеть

Для учебных компьютеров нужно предусмотреть профиль:

```bash
npm config set strict-ssl false
```

В проектном шаблоне это лучше хранить в `.npmrc` лабораторного окружения:

```ini
strict-ssl=false
registry=https://registry.npmjs.org/
```

Это компромисс для сети вуза. Для домашней и production-среды правильнее настроить корпоративный root CA через `cafile`.

---

## 9. Минимальная архитектура

- `content`: уроки, блоки, задания, тесты.
- `builder`: drag-and-drop, порядок блоков, параметры блоков.
- `generator`: сборка файлов проекта из блоков.
- `runner`: browser runner, Node runner, test runner.
- `diagnostics`: разбор ошибок и подсказки.
- `accounts`: прогресс, попытки, оплаты, группы.
- `teacher`: кабинеты преподавателей, дедлайны, отчеты.

### 9.1 Архитектура под маленький VPS

Целевой первый сервер: 2 ядра, 3.6 GHz, 10 GB свободного NVMe.

- Один web/API процесс: отдает SPA, API, авторизацию, прогресс, платежные webhooks.
- Одна SQLite база в WAL-режиме: `users`, `courses`, `lessons`, `blocks`, `attempts`, `payments`, `groups`.
- Один runner worker: берет задания из очереди и выполняет их изолированно.
- Browser runner остается на клиенте для простых JS-уроков, чтобы не грузить сервер.
- Node runner включается только для задач с файлами, тестами и npm-зависимостями.
- Concurrency runner по умолчанию: `1`; для 2 CPU можно поднять до `2`, если web/API остается отзывчивым.
- Ограничения job: 5 секунд CPU, 128-256 MB RAM, 1-2 MB stdout, временная папка до 20 MB.
- Сеть внутри runner выключена по умолчанию. Для npm install используем заранее подготовленный cache или отдельный controlled install step.
- Сборка образов идет в GitHub Actions; VPS только скачивает готовый image.

---

## 10. Что нужно доказать прототипом

1. Студент идет по маршруту проекта, а не выбирает блоки из общей кучи.
2. Каждый этап имеет цель, результат, ограниченный набор действий и проверку.
3. Сгенерированный код можно запустить.
4. Ошибка объясняется понятнее, чем стандартный stack trace.
5. Продвинутый режим дает ручное редактирование, а не закрытый no-code.
6. Интерфейс переносится на mobile без потери смысла.
7. Обязательные действия и улучшения визуально различаются.
8. Пояснения короткие: одно предложение "что делает" и одно предложение "зачем".

---

## 11. Модули будущего приложения

```text
apps/
  web/              # React/Vite UI, PWA, desktop/mobile layouts
  api/              # Fastify/Hono API, auth, progress, payments
  runner/           # isolated Node runner and diagnostics
packages/
  content-schema/   # lesson/block/task schemas
  generator/        # blocks -> project files
  diagnostics/      # JS error explanation rules
  ui/               # shared design system
content/
  js/
    lessons/
    blocks/
    projects/
deploy/
  vps/
```

Главное правило: `generator` и `diagnostics` должны быть библиотеками, а не кодом внутри UI. Тогда один и тот же движок будет работать в web, desktop wrapper, mobile wrapper и server-side runner.
